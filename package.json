{
  "name": "overtide",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "lint": "biome check apps packages package.json tsconfig.base.json biome.json",
    "format": "biome format --write .",
    "typecheck": "tsc -b apps/api packages/shared --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/bun": "1.3.14",
    "typescript": "^5.6.0"
  }
}
