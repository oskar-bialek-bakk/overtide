{
  "name": "overtide-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync": "node sync.mjs",
    "docs:dev": "node sync.mjs && vitepress dev",
    "docs:build": "node sync.mjs && vitepress build",
    "docs:preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.5.0"
  }
}
